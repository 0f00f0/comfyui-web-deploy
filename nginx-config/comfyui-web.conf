server {
    listen 80;
    server_name 139.196.236.28;

    location / {
        root /opt/comfyui-web/frontend;
        index index.html;
    }

    location /generate {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
